#ifndef HEADERS_H
#define HEADERS_H

#include <string>
#include <vector>
#include <sstream>
 
using namespace std;

vector<string> getWords(const string & s, char d = ' ');
double random(double min, double max);

#endif